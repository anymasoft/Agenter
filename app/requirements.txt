# Локальное приложение Agenter (backend + desktop в одном процессе).
# Зависимости совпадают с backend/desktop минус websockets (relay больше не нужен),
# плюс pywebview для нативного окна.

fastapi>=0.115
uvicorn[standard]>=0.32
anthropic>=0.40
aiohttp>=3.10
python-dotenv>=1.0
pywebview>=5.3
pydantic>=2.9
