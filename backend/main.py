import asyncio
import json
import logging
import os
import sqlite3
import uuid
from contextlib import asynccontextmanager
from datetime import datetime
from pathlib import Path
from typing import Any

from dotenv import load_dotenv

load_dotenv(Path(__file__).parent.parent / ".env")

import anthropic
from fastapi import BackgroundTasks, FastAPI, WebSocket, WebSocketDisconnect
from fastapi.middleware.cors import CORSMiddleware
from fastapi.staticfiles import StaticFiles
from pydantic import BaseModel

logging.basicConfig(level=logging.INFO, format="%(asctime)s %(levelname)s %(message)s")
log = logging.getLogger(__name__)

ANTHROPIC_API_KEY = os.environ.get("ANTHROPIC_API_KEY", "")

# Пути относительно расположения backend/
_BACKEND_DIR  = os.path.dirname(os.path.abspath(__file__))
_AGENTER_ROOT = os.path.dirname(_BACKEND_DIR)          # agenter/
DB_PATH       = os.path.join(_BACKEND_DIR, "agenter.db")
# Эффективный лимит turns (то что считается «полезной работой»). Считается
# orchestrator_sdk через свой счётчик — ask_user НЕ инкрементирует его, чтобы
# уточнение от юзера не «штрафовало» агента. Override через config.json:
# поле max_iterations. Опыт: на 9-шаговом ТЗ реалистично ~36-50 tool calls
# + резерв на ошибки/уточнения. 120 даёт запас на 2-3 крупных задачи.
MAX_ITERATIONS = 120

# Сколько раз агент может вызвать ask_user в одной задаче. Защита от петли
# «ошибка → ask_user → ответ → опять ошибка → ask_user → ...». После 5
# попыток PreToolUse hook блокирует следующий ask_user — агент обязан
# завершить задачу финальным сообщением.
MAX_ASK_USER_PER_TASK = 5

# Жёсткий потолок SDK max_turns — страховка от бесконечности на случай если
# наш собственный счётчик не сработает (баг в hook, runaway состояние).
# Должен быть заметно больше MAX_ITERATIONS + MAX_ASK_USER_PER_TASK.
SDK_HARD_CAP_TURNS = 200
MAX_TOOL_RESULT = 50_000

# Loop detection: если N последних tool calls идентичны (тот же
# tool + те же параметры) — задача останавливается принудительно.
# 3 — типичный размер петли, при которой ясно что LLM в тупике.
LOOP_DETECT_WINDOW = 3

# При ошибке этих инструментов pipeline немедленно прерывается
CRITICAL_TOOLS = {"db_dump", "db_load"}

# ────────────────────────────────────────────────────────────────
# SYSTEM_PROMPT — стиль «оркестратор», БЕЗ примеров BSL-кода.
#
# Принципы (НЕ нарушать при правках):
#   • НИ ОДНОГО BSL-сниппета. Никаких ```bsl ... ```.
#   • НИ ОДНОГО конкретного имени метода/свойства/объекта 1С
#     (Контрагенты, ИсточникДанных, НаборЗаписей и т.п.) как примера.
#   • Только: роль агента, decision tree «когда какой инструмент»,
#     workflows как последовательности шагов, запреты, контекст клиента.
#
# Знания о синтаксисе/API/БСП агент получает через инструменты:
#   - bsl_* (поиск в реальной конфигурации клиента — источник правды)
#   - skills meta_compile/meta_edit (help-вывод при неверных аргументах)
#   - в будущем: MCP-серверы для платформенного API, БСП, шаблонов
#
# Если LLM что-то не знает — она должна сначала искать через инструменты,
# и только если не нашла — спрашивать пользователя. НЕ изобретать имена.
#
# История: до 2026-05-15 промпт содержал ~770 строк примеров BSL —
# это был «костыль», компенсирующий отсутствие knowledge-MCP.
# Удалён как часть архитектурного перехода на data-driven knowledge layers.
# ────────────────────────────────────────────────────────────────
SYSTEM_PROMPT = """\
Ты — агент-оркестратор для разработки расширений 1С.

Твоя задача — НЕ помнить синтаксис 1С наизусть, а правильно выбирать
и применять инструменты (skills + MCP-серверы), которые знают конфигурацию,
платформенный API и БСП глубже тебя. Действуй как менеджер инструментов:
выбрал → вызвал → прочитал результат → решил что дальше.

═══════════════════════════════════════════════════════
 ОБЩИЙ ПРИНЦИП РАБОТЫ
═══════════════════════════════════════════════════════

МОДИФИЦИРУЮЩАЯ задача (создать/изменить/удалить):
  начало:   db_dump          (синхронизация ext_src ← БД)
  середина: подходящие skills/MCP под задачу
  конец:    cfe_validate → db_load

  Для БОЛЬШИХ задач (5+ объектов конфигурации) — раздели на ФАЗЫ.
  Каждая фаза = группа объектов, после которой БД остаётся валидной.
  В КОНЦЕ КАЖДОЙ ФАЗЫ обязательно cfe_validate → db_load (а не только в самом
  конце задачи). Это даёт промежуточные применения в БД и устойчивость к
  BUDGET EXHAUSTED — фазы 1..N-1 будут в БД даже если задача оборвалась.

  Признак границы фазы — изменения логически замкнуты:
    Фаза 1: справочники без зависимостей
    Фаза 2: регистры на этих справочниках
    Фаза 3: документы с реквизитами, ссылающимися на справочники/регистры
    Фаза 4: формы/проведение/отчёты, использующие документы

READ-ONLY задача (описать/найти/объяснить):
  db_dump (если данные могут устареть) → bsl_*/read_file → end_turn с ответом.
  Никаких cfe_validate / db_load.

ВЫБОР ИНСТРУМЕНТА (decision tree):
  Создать НОВЫЙ объект метаданных                → meta_compile
  Добавить/изменить РЕКВИЗИТ/ТЧ/свойство          → meta_edit
  Заимствовать ТИПОВОЙ объект из основной конф.  → cfe_borrow
  Перехватить МЕТОД заимствованного объекта      → cfe_patch_method
  Изменить BSL-код модуля                        → read_file + edit_file (или write_file для нового .bsl)
  Найти/прочитать существующий код или метаданные → bsl_* инструменты

Один инструмент за раз. Читай перед тем как писать.
Не пропускай db_dump в начале и cfe_validate перед db_load.

═══════════════════════════════════════════════════════
 WORKFLOWS ПО ТИПУ ЗАДАЧИ
═══════════════════════════════════════════════════════

A. Создание нового объекта в расширении:
   db_dump → meta_compile(definition) → cfe_validate → db_load

   meta_compile принимает JSON-описание (dict или массив dict для batch).
   Он сам генерирует XML по канонической структуре платформы и регистрирует
   объект в Configuration.xml. НЕ требует write_file для XML.

   Если не уверен в форме definition — вызови meta_compile с минимальным
   набором полей и прочитай его сообщение об ошибке: skill сам подскажет
   синтаксис.

B. Изменение существующего объекта (реквизиты/ТЧ/свойства):
   db_dump → meta_edit(object_path, operation, value) → cfe_validate → db_load

   Доступные operations skill объяснит при первом вызове. Если operation
   неизвестна — вызови meta_edit с заведомо-неполными аргументами и прочитай
   help-вывод.

C. Изменение BSL-кода модуля расширения:
   db_dump → (bsl_* для поиска образца) → read_file → edit_file → cfe_validate → db_load

   Перед написанием нетривиального BSL-кода (блокировки, движения регистров,
   запросы, перехваты, события документа) ОБЯЗАТЕЛЬНО найди реальный
   образец в основной конфигурации через bsl_code_grep / bsl_search_function
   и используй его структуру. Не сочиняй имена свойств и методов «из головы» —
   платформа 1С строгая, опечатки в именах свойств ловятся только при
   выполнении, не валидацией.

D. Заимствование типового объекта из основной конфигурации:
   db_dump → cfe_borrow(object) → (далее meta_edit / cfe_patch_method / edit_file)
          → cfe_validate → db_load

E. Перехват метода заимствованного объекта:
   db_dump → cfe_borrow (если нужно) → cfe_patch_method (создаёт каркас)
          → read_file + edit_file (дописать тело) → cfe_validate → db_load

F. Только анализ (read-only):
   db_dump (опционально) → bsl_* / read_file → end_turn с ответом.

═══════════════════════════════════════════════════════
 КАК ОБРАЩАТЬСЯ С ПРОБЕЛАМИ В ЗНАНИИ 1С
═══════════════════════════════════════════════════════

Ты НЕ обязан помнить:
  • точные имена методов и свойств платформы
  • полные сигнатуры API
  • имена и структуру стандартных подсистем (БСП)
  • полный список флагов реквизитов и свойств объектов метаданных
  • синтаксис конкретной операции meta_edit / meta_compile

Когда нужна такая информация:
  1. Сначала поищи в РЕАЛЬНОЙ конфигурации через bsl_* — это источник правды
     для данной клиентской базы (версия платформы, версия БСП, локальные
     модификации уже учтены).
  2. Если bsl_* не нашёл — попробуй вызвать целевой skill с минимальными
     аргументами: skills возвращают help-сообщения с описанием полей.
  3. Если задача требует знаний, которых нет ни в индексе, ни в skills —
     явно сообщи пользователю «нужен поиск по справке/БСП, такого
     инструмента сейчас нет» и предложи разбить задачу или уточнить.

НЕ изобретай имена. Лучше вернуться к пользователю с уточняющим вопросом,
чем закоммитить код с несуществующим методом.

═══════════════════════════════════════════════════════
 ИНСТРУМЕНТЫ — КРАТКАЯ КАРТА
═══════════════════════════════════════════════════════

──── Метаданные (skills) ────
  meta_compile(definition)                — создать новый объект по JSON.
                                            ОСНОВНОЙ инструмент создания.
                                            definition = dict или массив dict (batch).
  meta_edit(object_path, operation, value) — точечная правка существующего объекта
                                            (реквизит/ТЧ/измерение/ресурс/свойство).
  cfe_borrow(object, borrow_main?)         — заимствовать типовой объект в расширение.
                                            object: "Catalog.X" / "Document.Y" /
                                            "CommonModule.Z" / "Catalog.X.Form.Y" /
                                            batch через ' ;; '.
  cfe_patch_method(module_path, method,    — каркас перехватчика метода
                   interceptor_type, ...)     (Before / After / ModificationAndControl).
                                            Тело перехватчика дописывается потом
                                            через read_file + edit_file.
  cfe_validate()                           — полная валидация расширения.
                                            ОБЯЗАТЕЛЬНО перед db_load.

──── Платформенная справка 1С (строгая документация) ────
  platform_doc_lookup(name, limit=5)       — официальное описание метода/свойства/
                                            объекта/события платформы 1С из shcntx_ru.
                                            ОБЯЗАТЕЛЬНО перед использованием любого
                                            платформенного API. Возвращает signature,
                                            description, params, example.
                                            Если found=0 → метода нет в платформе.

──── Поиск/анализ конфигурации (BSL Atlas) ────
  Структурные (быстрые, всегда доступны):
    bsl_search_function(name, exact=true)    — найти функцию/процедуру по имени
    bsl_get_object(full_name)                — реквизиты, ТЧ, движения объекта
    bsl_code_grep(pattern, limit=20)         — поиск текста в BSL с контекстом функции
    bsl_get_function_context(function_name)  — граф вызовов
    bsl_get_module_functions(module_path)    — все функции конкретного модуля
    bsl_metadatasearch(query, limit=10)      — поиск объектов метаданных по названию

  Семантические (запрос на естественном языке):
    bsl_codesearch(query)                    — найти участки кода по смыслу
    bsl_helpsearch(query)                    — поиск в справке платформы 1С
    bsl_search_code_filtered(query, ...)     — семантический + структурные фильтры

  Стратегия: сначала структурные (если знаешь имя), потом семантические
  (если знаешь только смысл). Семантические могут вернуть "semantic mode disabled" —
  тогда переходи на bsl_code_grep с регексом.

──── Файловые операции ────
  list_ext_files()                         — список всех файлов расширения в ext_src/
  read_file(path)                          — прочитать файл из ext_src/ или SCHEME/
  edit_file(path, old_str, new_str)        — точечная замена (old_str точно совпадает)
  write_file(path, content)                — только BSL/текстовые; НИКОГДА для XML
                                            объектов метаданных

──── 1С операции ────
  db_dump()                                — выгрузить расширение из БД → ext_src/
                                            (всегда первым для актуальной задачи)
  db_load()                                — загрузить ext_src/ → БД
                                            (всегда последним для модифиц. задач)

──── Устаревшие (НЕ ИСПОЛЬЗОВАТЬ — есть для совместимости) ────
  clone_object, get_sample_object, validate_ext_structure
  Заменены на meta_compile / cfe_borrow / cfe_validate.

═══════════════════════════════════════════════════════
 ЖЁСТКИЕ ПРАВИЛА (HARD RULES)
═══════════════════════════════════════════════════════

Эти правила — НЕ рекомендации. Это обязательные предусловия.
Каждое нарушение = ошибка, которую LLM должна избегать.

R1. ВСЕГДА используй имеющиеся инструменты на 100%.
    Скилл (meta_compile/edit, cfe_*) или MCP (bsl_*) есть для каждой задачи.
    Перед тем как написать BSL-код вручную — проверь, нет ли скилла который
    решает то же самое за один вызов.

R2. ПЕРЕД ЛЮБЫМ нетривиальным BSL — обязательный поиск образца.
    Нетривиальное = упоминает любой из паттернов:
      • БлокировкаДанных, НаборЗаписей, Движения.*
      • РегистрыНакопления/Сведений, виртуальные таблицы (Остатки/Обороты/СрезПоследних)
      • Запрос, ВЫБРАТЬ, Транзакция, Попытка
      • События документа (ПередЗаписью, ПриЗаписи, ОбработкаПроведения)
      • Формы (РеквизитФормыВЗначение, ЗначениеВРеквизитФормы, Элементы.*.Видимость)
      • &Перед, &После, &Вместо, ПродолжитьВызов
      • HTTPСервисОтвет, ЧтениеJSON, ЗаписьJSON, XMLЗначение
    Шаги: (1) bsl_code_grep с паттерном или (2) bsl_search_function по имени.
    НАШЁЛ образец — копируй его структуру, адаптируй имена.
    НЕ НАШЁЛ — попробуй другой запрос. Всё ещё пусто — сообщи пользователю,
    НЕ ВЫДУМЫВАЙ имена и сигнатуры.

R3. ПЕРЕД СОЗДАНИЕМ объекта — только meta_compile.
    Любая мысль «склонирую/скопирую/напишу XML руками» = неправильный маршрут.
    meta_compile принимает JSON и генерирует корректный XML + регистрирует
    в Configuration.xml. clone_object и get_sample_object — DEPRECATED.

R4. ПЕРЕД ИЗМЕНЕНИЕМ существующего объекта (реквизиты/ТЧ/свойства) — meta_edit.
    НЕ используй read_file + edit_file для XML метаданных.
    meta_edit покрывает: add/remove/modify-attribute, -ts, -dimension, -resource,
    -enumValue, modify-property, add-owner, add-registerRecord, etc.

R5. ПЕРЕД ЗАИМСТВОВАНИЕМ типового объекта — bsl_metadatasearch + cfe_borrow.
    Сначала убедись что объект существует (metadatasearch), потом cfe_borrow.
    bsl_get_object перед borrow — увидишь состав объекта.

R6. ПЕРЕД ПЕРЕХВАТОМ метода — bsl_get_module_functions для проверки сигнатуры.
    cfe_patch_method создаёт только каркас — тело дописывается через
    read_file + edit_file ПОСЛЕ изучения оригинального метода.

R7. ПЕРЕД db_load — обязательный cfe_validate в той же задаче.
    Если cfe_validate показал errors — НЕ вызывай db_load. Сначала исправь.

R8. ВСЕГДА завершай модифицирующую задачу. Никаких «оставлю как есть».
    Задача считается выполненной только после успешного db_load.
    Read-only задача (анализ/описание) — db_load не нужен, заверши end_turn.

R9. ЕСЛИ tool вернул пусто/ошибку — изменяй подход, а НЕ выдумывай.
    Пусто из bsl_search_function → пробуй bsl_code_grep с regex.
    Пусто из bsl_code_grep → попробуй другой паттерн.
    Всё пусто → сообщи пользователю «не нашёл, нужна подсказка», НЕ продолжай.
    cfe_validate с errors → читай errors, точечно исправляй, повторяй.

R10. ОДИН ИНСТРУМЕНТ ЗА РАЗ. Не вызывай batch произвольно — обдумывай результат
    каждого вызова перед следующим. Скиллы поддерживают batch через ';;' —
    это другое (один скилл, много операций), это разрешено.

R11. ПЕРЕД ИСПОЛЬЗОВАНИЕМ ЛЮБОГО ПЛАТФОРМЕННОГО МЕТОДА/СВОЙСТВА — обязательный
    platform_doc_lookup. Это источник правды по платформе 1С (shcntx_ru от 1С,
    проиндексирован локально в Agenter). Перед тем как написать в BSL:
      • БлокировкаДанных, БлокировкаДанных.ИсточникДанных
      • НаборЗаписей.Записать, НаборЗаписей.Добавить
      • Запрос.Выполнить, Запрос.УстановитьПараметр
      • РеквизитФормыВЗначение, ЗначениеВРеквизитФормы
      • любой метод/свойство платформы, в имени которого ты не уверен на 100%
    → platform_doc_lookup("ИмяМетода") → получи строгую сигнатуру и описание.
    Если found=0 — этого метода НЕ существует. НЕ выдумывай. Возможно это
    метод БСП — проверь через bsl_search_function. Иначе — спроси пользователя.

    ВАЖНО: platform_doc_lookup даёт ОФИЦИАЛЬНУЮ сигнатуру параметров. Если ты
    «помнишь» что метод вызывается по-другому — официальная сигнатура важнее
    твоей памяти. Всегда копируй имена параметров из platform_doc_lookup.

R12. ФОРМЫ (Form.xml) — ТОЛЬКО через form_* tools. ЗАПРЕЩЕНО редактировать
     Form.xml через Edit / Write / Bash regex — XML формы содержит ~17 namespace
     деклараций, ID-пулы, companion-элементы (ContextMenu, ExtendedTooltip) и
     множество правил, которые Edit'ом не повторишь без ошибок.
     Канонический workflow для форм:
       • form_info(form_path)             — посмотреть структуру (вместо Read)
       • form_compile(output_path, ...)   — создать с нуля из JSON-DSL
       • form_edit(form_path, json_path)  — добавить элементы/реквизиты/команды
       • form_validate(form_path)         — локальная валидация ДО cfe_validate
     Если знаний JSON-DSL не хватает — посмотри SKILL.md скилла через
     skill_search('форма') или Read .claude/skills/form-compile/SKILL.md.

R13. ВАЛИДАЦИЯ В НЕСКОЛЬКО УРОВНЕЙ. Перед дорогим cfe_validate → db_load —
     прогоняй дешёвые локальные проверки на изменённые объекты:
       • meta_validate(object_path)  — для одного объекта метаданных
       • form_validate(form_path)    — для формы
       • mcp__syntax_check__check    — для BSL-кода (если SyntaxCheckServer
         подключён в config.json и Docker контейнер запущен)
     Это ловит ~70% ошибок локально, без обращения к 1С — экономит turns.

R14. ИМЕНА ОБЪЕКТОВ — ТОЛЬКО через bsl_* / metadatasearch / meta_info /
     skill_search. ЗАПРЕЩЕНО искать имена через Bash python+os.walk по SCHEME:
     папки SCHEME в cp1251, Python показывает крокозябры, тратится 5+ turns
     впустую. Правильный поиск:
       • bsl_search_function("ИмяФункции")    — для функций/процедур
       • bsl_metadatasearch("Контрагенты")    — для объектов метаданных
       • bsl_get_object_details("Document.X") — для реквизитов/ТЧ объекта
       • meta_info(object_path)                — для одного известного объекта

R15. ТОПОЛОГИЯ ОБЪЕКТОВ. Если в одной задаче создаются объекты A и B, и B
     ссылается на A (CatalogRef.A в реквизите B) — meta_compile(A) ДОЛЖЕН
     быть строго до meta_compile(B). Иначе PRE-FLIGHT блокирует B, потому
     что справочника A ещё нет.
     Если жёстко нужен batch — создавай A с явной заглушкой и потом
     meta_edit(B) с реальным типом. Лучше всё-таки порядок.

R16. CATALOG ИНСТРУМЕНТОВ. Перед тем как идти в Edit / Bash для нестандартной
     операции (макет MXL, СКД, роль, подсистема, обработка, веб-публикация и
     т.п.) — ВСЕГДА сначала skill_search("что нужно сделать"). На диске лежит
     ~60 готовых протестированных скиллов. Если есть подходящий — skill_run
     (см. skill_search hint по аргументам). Это дисциплинирует «не изобретать».

═══════════════════════════════════════════════════════
 АБСОЛЮТНЫЕ ЗАПРЕТЫ
═══════════════════════════════════════════════════════

✗ НЕ вызывай clone_object, get_sample_object, validate_ext_structure — это
   DEPRECATED tools. Их замены: meta_compile, meta_edit (без образцов), cfe_validate.
   Если рассматриваешь эти tools — это сигнал что маршрут неправильный.
✗ НЕ создавай XML-файлы объектов метаданных через write_file. Используй
   meta_compile для создания, meta_edit для правки. write_file разрешён ТОЛЬКО
   для BSL/текстовых файлов.
✗ НЕ редактируй XML метаданных через read_file + edit_file там, где работает meta_edit.
✗ НЕ вызывай edit_file без предварительного read_file — old_str не совпадёт.
✗ НЕ читай SCHEME/ напрямую через read_file для поиска образцов — используй
   bsl_* инструменты (code_grep / search_function / get_object / metadatasearch).
✗ НЕ трогай ObjectModule.bsl документов без явного указания пользователя
   (ведётся пользователем самостоятельно).
✗ Для модифицирующих задач: НЕ пропускай db_dump в начале и cfe_validate → db_load в конце.
✗ НЕ вызывай db_load если cfe_validate показал errors — сначала исправь точечно.
✗ НЕ сочиняй имена методов, свойств, объектов платформы «из головы». Проверь
   через bsl_* в реальной конфигурации; если там нет — спроси у пользователя.
   Особенно опасные категории фантазий: имена свойств БлокировкаДанных,
   методы НаборЗаписей, имена событий документа, имена реквизитов форм.
✗ НЕ изобретай имена объектов метаданных. Если пользователь не назвал
   точное имя — bsl_metadatasearch для проверки существования.
✗ НЕ продолжай задачу когда инструмент вернул ошибку или пусто. Меняй подход
   (другой инструмент, другой запрос) или останавливайся и спрашивай пользователя.
✗ НЕ редактируй Form.xml через Edit / Write / Bash — XML формы не редактируется
   регексом. Form.xml только через form_compile / form_edit (см. R12).
✗ НЕ используй Bash с python+os.walk для поиска по SCHEME — кодировка ломает
   имена. bsl_* / metadatasearch / meta_info (см. R14).
✗ НЕ изобретай порядок создания объектов — соблюдай топологию ссылок (R15).

═══════════════════════════════════════════════════════
 ДИСПЕТЧЕРИЗАЦИЯ СТАДИЙ — STAGE-DISPATCH (R21)
═══════════════════════════════════════════════════════

Sprint 4 архитектура: ты НЕ выбираешь tool свободно. Ты КЛАССИФИЦИРУЕШЬ
стадию задачи, диспетчер сам определяет какой tool ты должен вызвать.

## ПОТОК РАБОТЫ ДЛЯ L2+ ЗАДАЧ

  1. **ПЕРВЫЙ ШАГ** — `plan_task(stages=[...])`. ТЗ → массив стадий, у каждой:
       • `kind` — один из 22 фиксированных типов (см. описание plan_task)
       • `description` — что именно делать
       • `args_hint` (опц.) — подсказка args для будущего вызова

  2. Каждая стадия имеет **expected_tool**, который определяется диспетчером
     детерминистически из её `kind`. Ты этот tool ВИДИШЬ в ответе plan_task.

  3. На активной стадии ты **обязан** вызвать именно expected_tool.
     Любой другой модифицирующий tool блокируется guard'ом с подсказкой.

  4. После успешного expected_tool стадия авто-помечается completed,
     активируется следующая pending.

  5. Финальная стадия обычно `validate-and-load` → cfe_validate + db_load.

## ЧТО РАЗРЕШЕНО НА ЛЮБОЙ СТАДИИ (без ограничений)

  • read-only: Read, Glob, Grep, bsl_atlas_*, meta_info, form_info,
    platform_doc_lookup, platform_doc_search
  • валидация: cfe_validate, meta_validate, form_validate, syntax_check
  • discovery: skill_search
  • interaction: ask_user
  • переплан: plan_task (если оказалось что стадии надо перестроить)
  • sub-agent: Agent (для делегирования узкой работы)

## КОГДА МЕНЯТЬ ПЛАН

Если в процессе ты понял что план был неточным:
  • вызови plan_task ПОВТОРНО с обновлённым массивом стадий
  • стадии переписываются (старые удаляются), активируется новая стадия 1
  • это нормально — плохо НЕ переписать план, оставшись с неверным

## КОГДА ИСПОЛЬЗОВАТЬ kind='other'

Только если ни один из 22 стандартных типов не подходит. Это сигнал что
тебе понадобится skill_search для подбора инструмента. Не злоупотребляй —
почти любая 1С-операция вписывается в стандартные kind'ы.

## АНТИ-ПАТТЕРНЫ (НЕ ДЕЛАЙ)

  ✗ Edit на XML вообще (Form.xml, Subsystems/*.xml, Catalogs/*.xml) —
    у каждого свой kind стадии и свой expected_tool.
  ✗ «Я попробую сам через Bash» вместо ask_user/skill_search при
    неоднозначности — это нарушение R20.
  ✗ Игнорировать сообщения guard'а — он указывает правильный путь.
  ✗ Удалять заимствованные объекты «потому что не получилось» — лучше
    откатить план через plan_task.

═══════════════════════════════════════════════════════
 АНТИ-ГАЛЛЮЦИНАЦИЯ ОГРАНИЧЕНИЙ ПЛАТФОРМЫ (R20)
═══════════════════════════════════════════════════════

Если ты собираешься сказать «расширения 1С не позволяют ...», «в платформе
это невозможно», «нельзя добавить ... к заимствованному ...» — это **СИГНАЛ
ОСТАНОВИТЬСЯ**. С вероятностью 95% ты выбрал НЕПРАВИЛЬНЫЙ инструмент, а
не упёрся в реальное ограничение платформы.

Расширения 1С позволяют:
  ✓ Добавлять объекты в Content заимствованной подсистемы (через subsystem-edit
    skill, операция add-content). НЕ через Edit XML.
  ✓ Добавлять реквизиты и табличные части к заимствованным объектам
    (через meta_edit с object_path).
  ✓ Перехватывать любой метод (cfe_patch_method).
  ✓ Заимствовать формы и дополнять элементами (form_edit с extension-режимом).
  ✓ Создавать собственные роли и подсистемы.

Прежде чем сказать «невозможно»:
  1. skill_search с описанием задачи — наверняка есть готовый скилл.
  2. ask_user — спроси юзера если правда не понятно.
  3. platform_doc_lookup — проверь существование нужного API.

Если ты УЖЕ потратил 3+ turns на безуспешные обходы — это сильнейший сигнал
что подход неправильный. ОСТАНОВИСЬ и переключись на skill_search или ask_user.
НЕ удаляй заимствованные объекты «потому что не получилось» — это потеря
работы пользователя.

═══════════════════════════════════════════════════════
 ВЕДЕНИЕ TODOWRITE (R18)
═══════════════════════════════════════════════════════

Если ты вызвал TodoWrite — поддерживай статусы пунктов АКТУАЛЬНЫМИ:
  • Сразу как берёшь пункт в работу — TodoWrite со статусом `in_progress`
  • Сразу как закончил пункт — TodoWrite со статусом `completed`
  • Если решил пропустить пункт — статус `cancelled` (НЕ оставляй pending)

Это влияет на финальный статус задачи в UI: незавершённые pending/in_progress
пункты на финале = «частично применено». Если все статусы actualized — UI
покажет «применено в БД». Не оставляй неактуальные статусы — это даёт
пользователю ложное представление о результате.

Если TodoWrite не нужен (маленькая задача L1/L2) — не вызывай его вообще.
Полное отсутствие TodoWrite = задача оценивается только по db_load.

═══════════════════════════════════════════════════════
 SUB-AGENT ДЕЛЕГИРОВАНИЕ (R17)
═══════════════════════════════════════════════════════

Sprint 3 S3.1: для УЗКИХ подзадач большой работы используй встроенный
`Agent` tool — он спавнит отдельного агента с чистым контекстом и
урезанной зоной ответственности. Это ускоряет L3-L4 задачи: каждый
sub-agent держит фокус, не отвлекаясь на остальные пункты ТЗ.

ПРЕДОПРЕДЕЛЁННЫЕ РОЛИ И КОГДА ИХ ИСПОЛЬЗОВАТЬ:

**form_builder** — спецализируется на создании / правке управляемых форм
   Когда вызывать: одна сложная форма с 10+ элементами или extension-формы
   с обработчиками. Tools которыми пользуется: form_*, meta_info, Read.
   Prompt-стиль: «Сгенерируй Form.xml для документа X со следующими
   элементами: ...». Возвращает: путь к созданному/изменённому Form.xml
   + краткий отчёт.

**module_writer** — пишет BSL-модули объектов или модули форм
   Когда вызывать: модуль объекта на 100+ строк, отдельный
   общий модуль, обработчик проведения. Tools: Read, Edit, Write,
   platform_doc_lookup, bsl_search_function, syntax_check.
   Prompt-стиль: «Напиши модуль формы для X с обработчиками A, B, C».

**validator** — финальная проверка всего расширения перед db_load
   Когда вызывать: после крупной фазы, перед финальным db_load. Tools:
   cfe_validate, meta_validate, form_validate, syntax_check (на изменённых
   .bsl). Prompt-стиль: «Проверь расширение и список .bsl-файлов: [...]».
   Возвращает структурированный отчёт об ошибках.

ВЫЗОВ:

    Agent({
      subagent_type: "general-purpose",  // или специализированный если есть
      description: "Build РасходТовара form",
      prompt: "<подробный prompt для роли>"
    })

КОГДА НЕ ИСПОЛЬЗОВАТЬ:
  ✗ Маленькая задача целиком (L1/L2) — overhead не оправдан.
  ✗ Задачи, требующие много context'а из основного диалога — sub-agent
    его не получает, ему нужно явное описание.
  ✗ Когда работа уже идёт линейно и быстро — не дроби ради дробления.

ЛИМИТЫ:
  • На каждую задачу — не более 3-4 sub-agent вызовов суммарно.
  • Каждый sub-agent — не более ~15 turns внутри (он отдельный SDK
    клиент со своим бюджетом).

═══════════════════════════════════════════════════════
 КОНТЕКСТ КЛИЕНТА
═══════════════════════════════════════════════════════

Конкретные пути (ext_src, SCHEME, база, платформа), имя расширения
и префикс инжектятся в этот промпт в runtime — см. блок ниже («КОНТЕКСТ
ТЕКУЩЕГО КЛИЕНТА»). Все НОВЫЕ объекты именуй с префиксом расширения.
"""


def _extract_prefix(extension_name: str) -> str:
    """Извлекает префикс из имени расширения (например 'расш_' из 'РасшМоёРасширение')."""
    import re
    m = re.match(r"([A-Za-zА-Яа-яЁё]+_)", extension_name)
    if m:
        return m.group(1).lower()
    return ""


def _build_system_prompt(client_cfg: dict | None = None) -> str:
    """Строит системный промпт с добавлением контекста клиента если есть."""
    if not client_cfg:
        return SYSTEM_PROMPT
    ext      = client_cfg.get("extension", "")
    ext_src  = client_cfg.get("ext_src_path", "")
    scheme   = client_cfg.get("scheme_path", "")
    base     = client_cfg.get("base_path", "")
    v8       = client_cfg.get("v8_path", "")
    prefix   = _extract_prefix(ext)
    ctx = f"""

═══════════════════════════════════════════════════════
 КОНТЕКСТ ТЕКУЩЕГО КЛИЕНТА
═══════════════════════════════════════════════════════

  Расширение : {ext or "(не задано)"}
  Префикс    : {prefix or "(определить из кода)"}
  ext_src    : {ext_src or "(не задан)"}
  SCHEME     : {scheme or "(не задан — образцы брать из ext_src или bsl_* инструментов)"}
  База 1С    : {base or "(не задана)"}
  Платформа  : {v8 or "(не задана)"}

Все новые объекты расширения именуй с префиксом «{prefix or "???_"}».
При изменении существующих объектов базы — префикс в имени метода обязателен.
При чтении из SCHEME: read_file("SCHEME/Catalogs/ИмяОбъекта/ИмяОбъекта.xml").
"""
    return SYSTEM_PROMPT + ctx

TOOL_DEFINITIONS = [
    {
        "name": "db_dump",
        "description": "Синхронизировать БД 1С → ext_src/ (XML файлы). ОБЯЗАТЕЛЬНО вызывать первым.",
        "input_schema": {"type": "object", "properties": {}, "required": []},
    },
    {
        "name": "db_load",
        "description": (
            "Загрузить ext_src/ → БД 1С. ФИНАЛЬНЫЙ шаг модифицирующей задачи.\n"
            "ОБЯЗАТЕЛЬНЫЕ предусловия (если хотя бы одно НЕ выполнено — НЕ вызывай):\n"
            "  (1) cfe_validate выполнен в этой же задаче и вернул 0 errors\n"
            "  (2) Все запланированные изменения применены\n"
            "  (3) Если cfe_validate показал warnings — они проанализированы\n"
            "ЕСЛИ cfe_validate вернул errors — НЕ вызывай db_load. Сначала исправь."
        ),
        "input_schema": {"type": "object", "properties": {}, "required": []},
    },
    {
        "name": "bsl_search_function",
        "description": (
            "Найти функцию/процедуру BSL по имени во всех модулях основной конфигурации "
            "(включая БСП). Use BEFORE writing any BSL-code which references a function/method:\n"
            "  • Не уверен в имени метода? — bsl_search_function ПЕРЕД написанием.\n"
            "  • Хочешь использовать функцию БСП? — найди её и убедись что существует.\n"
            "  • Нашёл? — посмотри её сигнатуру через bsl_get_module_functions.\n"
            "  • НЕ нашёл с exact=true? — попробуй exact=false (поиск подстрокой) или bsl_code_grep.\n"
            "  • Всё ещё не нашёл? — функции не существует. НЕ ВЫДУМЫВАЙ. Сообщи пользователю."
        ),
        "input_schema": {
            "type": "object",
            "properties": {
                "name": {"type": "string", "description": "Имя функции/процедуры точно как в коде"},
                "exact": {"type": "boolean", "default": True, "description": "true=строгое равенство, false=поиск подстрокой"},
            },
            "required": ["name"],
        },
    },
    {
        "name": "bsl_get_object",
        "description": (
            "Получить реквизиты, ТЧ, измерения, ресурсы и движения объекта метаданных 1С. "
            "Use BEFORE:\n"
            "  • заимствования объекта (cfe_borrow) — чтобы знать состав\n"
            "  • добавления реквизита к существующему объекту — чтобы не дублировать\n"
            "  • написания кода, обращающегося к ТЧ/реквизитам — чтобы знать точные имена\n"
            "  • любой ссылки на чужой объект в BSL — чтобы знать какие у него поля"
        ),
        "input_schema": {
            "type": "object",
            "properties": {"full_name": {"type": "string", "description": "Полное имя: 'Catalog.Контрагенты' / 'Document.X' / 'AccumulationRegister.Y'"}},
            "required": ["full_name"],
        },
    },
    {
        "name": "bsl_code_grep",
        "description": (
            "Поиск текста/regex-паттерна в BSL-коде SCHEME с контекстом функции.\n"
            "Use когда:\n"
            "  • bsl_search_function не дал ответа (не знаешь точного имени)\n"
            "  • Ищешь паттерн использования (например 'БлокировкаДанных' или 'НаборЗаписей.*Записать')\n"
            "  • Хочешь увидеть КАК типовой код решает похожую задачу — найди образец и адаптируй\n"
            "  • Перед написанием НЕТРИВИАЛЬНОГО BSL (блокировки, движения, запросы, формы) — "
            "    ОБЯЗАТЕЛЬНО найди живой образец в основной конфигурации\n"
            "Возвращает фрагменты с именем функции, в которой найден паттерн.\n"
            "Если результат пуст — паттерн не используется в конфигурации. НЕ выдумывай — спроси пользователя."
        ),
        "input_schema": {
            "type": "object",
            "properties": {
                "pattern": {"type": "string", "description": "Текст или regex для поиска"},
                "case_sensitive": {"type": "boolean", "default": False},
                "limit": {"type": "integer", "default": 20},
            },
            "required": ["pattern"],
        },
    },
    {
        "name": "bsl_get_function_context",
        "description": (
            "Граф вызовов функции: ЧТО она вызывает и КТО её вызывает (callers/callees). "
            "Use когда:\n"
            "  • Меняешь сигнатуру функции — нужно проверить кто её вызывает\n"
            "  • Понимаешь поведение через граф зависимостей\n"
            "  • Заимствуешь общий модуль и хочешь увидеть его связи"
        ),
        "input_schema": {
            "type": "object",
            "properties": {"function_name": {"type": "string"}},
            "required": ["function_name"],
        },
    },
    {
        "name": "bsl_get_module_functions",
        "description": (
            "Список всех функций/процедур одного модуля + их сигнатуры. "
            "Use когда:\n"
            "  • Заимствовал модуль через cfe_borrow и нужно выбрать какой метод перехватывать\n"
            "  • Хочешь увидеть весь API общего модуля БСП перед использованием\n"
            "  • Анализируешь типовой модуль перед изменением"
        ),
        "input_schema": {
            "type": "object",
            "properties": {"module_path": {"type": "string", "description": "Путь к модулю или его часть (поиск подстрокой)"}},
            "required": ["module_path"],
        },
    },
    {
        "name": "bsl_metadatasearch",
        "description": (
            "Поиск объектов метаданных 1С (Catalog, Document, регистры, перечисления) по названию. "
            "Use BEFORE:\n"
            "  • Заимствования объекта — убедись что он существует и правильно назван\n"
            "  • Создания связи (CatalogRef.X) — проверь что X действительно существует\n"
            "  • Использования регистра в коде — проверь точное имя\n"
            "Если запрос дал пусто — объекта НЕТ в конфигурации. НЕ выдумывай — спроси пользователя."
        ),
        "input_schema": {
            "type": "object",
            "properties": {
                "query": {"type": "string", "description": "Имя или часть имени объекта"},
                "limit": {"type": "integer", "default": 10},
            },
            "required": ["query"],
        },
    },
    {
        "name": "bsl_codesearch",
        "description": (
            "Семантический поиск по BSL-коду конфигурации. Принимает запрос "
            "на естественном языке («блокировка остатков перед движениями», "
            "«создание документа и проведение») и находит участки кода с "
            "похожей семантикой. Используй когда не знаешь имени конкретной "
            "функции, но понимаешь ЧТО ищешь. Требует semantic-режим BSL Atlas — "
            "если вернёт ошибку, переключись на bsl_code_grep или bsl_search_function."
        ),
        "input_schema": {
            "type": "object",
            "properties": {
                "query": {"type": "string", "description": "Запрос на естественном языке"},
                "limit": {"type": "integer", "default": 10},
            },
            "required": ["query"],
        },
    },
    {
        "name": "bsl_helpsearch",
        "description": (
            "Поиск в документации платформы 1С. Используй для уточнения сигнатур "
            "методов, имён свойств, поведения встроенных объектов 1С. "
            "Возвращает релевантные фрагменты официальной справки. Требует "
            "semantic-режим BSL Atlas — если ошибка, ищи образец через bsl_code_grep."
        ),
        "input_schema": {
            "type": "object",
            "properties": {
                "query": {"type": "string", "description": "Что искать в справке (имя метода, концепция)"},
                "limit": {"type": "integer", "default": 10},
            },
            "required": ["query"],
        },
    },
    {
        "name": "bsl_search_code_filtered",
        "description": (
            "Семантический поиск по коду + структурные фильтры. Используй когда "
            "хочешь найти паттерн ИМЕННО в определённом контексте: только формы, "
            "только модули объектов, только функции по шаблону имени. Требует "
            "semantic-режим BSL Atlas."
        ),
        "input_schema": {
            "type": "object",
            "properties": {
                "query": {"type": "string", "description": "Семантический запрос"},
                "module_type": {
                    "type": "string",
                    "description": "Фильтр по типу модуля: ObjectModule | ManagerModule | FormModule | CommonModule",
                },
                "function_name_pattern": {
                    "type": "string",
                    "description": "Регулярка по имени функции",
                },
                "object_type": {
                    "type": "string",
                    "description": "Фильтр по типу объекта метаданных: Catalog | Document | …",
                },
                "limit": {"type": "integer", "default": 10},
            },
            "required": ["query"],
        },
    },
    {
        "name": "read_file",
        "description": "Прочитать файл из ext_src/.",
        "input_schema": {
            "type": "object",
            "properties": {"path": {"type": "string"}},
            "required": ["path"],
        },
    },
    {
        "name": "edit_file",
        "description": "Заменить old_str на new_str в файле (точное совпадение обязательно).",
        "input_schema": {
            "type": "object",
            "properties": {
                "path": {"type": "string"},
                "old_str": {"type": "string"},
                "new_str": {"type": "string"},
            },
            "required": ["path", "old_str", "new_str"],
        },
    },
    {
        "name": "write_file",
        "description": (
            "Создать или перезаписать файл в ext_src/. "
            "ТОЛЬКО для BSL-кода (*.bsl) и текстовых файлов. "
            "ЗАПРЕЩЕНО использовать для создания XML-файлов объектов метаданных 1С "
            "(Catalog.xml, Document.xml и подобных) — для этого используй clone_object."
        ),
        "input_schema": {
            "type": "object",
            "properties": {
                "path": {"type": "string"},
                "content": {"type": "string"},
            },
            "required": ["path", "content"],
        },
    },
    {
        "name": "list_ext_files",
        "description": "Список всех файлов расширения в ext_src/.",
        "input_schema": {"type": "object", "properties": {}, "required": []},
    },
    {
        "name": "get_sample_object",
        "description": (
            "[DEPRECATED — НЕ ИСПОЛЬЗОВАТЬ] "
            "Этот инструмент остался для совместимости. Для создания нового объекта "
            "используй ТОЛЬКО meta_compile — он не требует образца, генерирует XML "
            "по канонической структуре платформы. Любой вызов get_sample_object — "
            "признак неправильного маршрута."
        ),
        "input_schema": {
            "type": "object",
            "properties": {
                "object_type": {"type": "string"},
            },
            "required": ["object_type"],
        },
    },
    {
        "name": "clone_object",
        "description": (
            "[DEPRECATED — НЕ ИСПОЛЬЗОВАТЬ] "
            "Этот инструмент остался для совместимости. Для создания нового объекта "
            "метаданных используй ТОЛЬКО meta_compile — он генерирует правильный XML "
            "автоматически по JSON-описанию, без копирования образцов и ручной правки UUID. "
            "Любой вызов clone_object — ошибка маршрута: переключись на meta_compile."
        ),
        "input_schema": {
            "type": "object",
            "properties": {
                "source_path": {"type": "string"},
                "new_name": {"type": "string"},
            },
            "required": ["source_path", "new_name"],
        },
    },
    {
        "name": "validate_ext_structure",
        "description": (
            "[DEPRECATED — НЕ ИСПОЛЬЗОВАТЬ] "
            "Заменён на cfe_validate, который делает полную проверку. "
            "Любой вызов validate_ext_structure — переключись на cfe_validate."
        ),
        "input_schema": {"type": "object", "properties": {}, "required": []},
    },
    # ─────────────────────────────────────────────────────────────────────
    # Подключённые скиллы 1С — основные инструменты для работы с метаданными
    # ─────────────────────────────────────────────────────────────────────
    {
        "name": "meta_compile",
        "description": (
            "ОСНОВНОЙ инструмент СОЗДАНИЯ новых объектов метаданных. "
            "Принимает JSON-описание и генерирует правильный XML + регистрирует объект "
            "в Configuration.xml. Поддерживает все типы: Catalog, Document, "
            "InformationRegister, AccumulationRegister, AccountingRegister, Enum, "
            "Constant, CommonModule, HTTPService, Report, DataProcessor, ExchangePlan и др.\n"
            "Минимальный JSON: {\"type\": \"Catalog\", \"name\": \"МойСправочник\"}\n"
            "С реквизитами: {\"type\": \"Catalog\", \"name\": \"X\", \"attributes\": [\"ИНН: String(12)\", \"Сумма: Number(15,2) | req, index\"]}\n"
            "С ТЧ: {\"type\": \"Document\", \"name\": \"X\", \"tabularSections\": {\"Товары\": [\"Номенклатура: CatalogRef.Номенклатура\", \"Кол: Number(15,3)\"]}}\n"
            "Регистр: {\"type\": \"InformationRegister\", \"name\": \"X\", \"dimensions\": [...], \"resources\": [...]}\n"
            "Batch: массив [{...}, {...}].\n"
            "ВАЖНО: ИМЯ ДОЛЖНО НАЧИНАТЬСЯ С ПРЕФИКСА расширения (см. контекст клиента)."
        ),
        "input_schema": {
            "type": "object",
            "properties": {
                "definition": {
                    "description": "JSON-описание одного объекта (object) или нескольких (array of objects)",
                },
            },
            "required": ["definition"],
        },
    },
    {
        "name": "meta_edit",
        "description": (
            "ТОЧЕЧНОЕ РЕДАКТИРОВАНИЕ существующего объекта метаданных. "
            "ПРЕДПОЧТИТЕЛЬНЕЕ чем read_file+edit_file для типовых операций: "
            "добавить/удалить/переименовать реквизит, ТЧ, измерение, ресурс, "
            "изменить свойство объекта. Скилл сам разберёт XML и сделает правку безопасно.\n"
            "Примеры:\n"
            "  add-attribute, value=\"ИНН: String(12) | req\"\n"
            "  add-ts, value=\"Товары: Ном: CatalogRef.Ном, Кол: Number(15,3)\"\n"
            "  add-dimension, value=\"Организация: CatalogRef.Организации | master\"\n"
            "  modify-property, value=\"CodeLength=11 ;; DescriptionLength=150\"\n"
            "  remove-attribute, value=\"СтарыйРеквизит\"\n"
            "Batch через ';;'."
        ),
        "input_schema": {
            "type": "object",
            "properties": {
                "object_path": {
                    "type": "string",
                    "description": "Путь к объекту в ext_src, например 'Catalogs/МойСправочник' или 'Documents/Заказ'",
                },
                "operation": {
                    "type": "string",
                    "description": (
                        "Операция: add-attribute / add-ts / add-dimension / add-resource / "
                        "add-enumValue / add-column / add-form / add-template / add-command / "
                        "add-ts-attribute / remove-attribute / remove-ts / remove-ts-attribute / "
                        "modify-attribute / modify-ts-attribute / modify-ts / modify-property / "
                        "add-owner / add-registerRecord / add-basedOn / set-owners / "
                        "set-registerRecords / set-basedOn"
                    ),
                },
                "value": {
                    "type": "string",
                    "description": "Значение в формате DSL скилла (см. описание инструмента). Batch через ';;'",
                },
            },
            "required": ["object_path", "operation", "value"],
        },
    },
    {
        "name": "cfe_borrow",
        "description": (
            "ЗАИМСТВОВАНИЕ объекта из основной конфигурации в расширение. "
            "Нужно ПЕРЕД тем как добавлять реквизиты к существующему типовому объекту "
            "или перехватывать его методы. Создаёт XML с ObjectBelonging=Adopted и "
            "регистрирует в Configuration.xml.\n"
            "Формат object: 'Catalog.Контрагенты' / 'Document.РТУ' / 'CommonModule.ОбщегоНазначения' / "
            "'Catalog.Контрагенты.Form.ФормаЭлемента' (для заимствования формы). Batch через ';;'.\n"
            "Для форм с реквизитами: borrow_main_attribute=\"Form\" (реквизиты с формы) или \"All\" (все)."
        ),
        "input_schema": {
            "type": "object",
            "properties": {
                "object": {
                    "type": "string",
                    "description": "Полное имя заимствуемого объекта в нотации Type.Name[.Form.FormName]",
                },
                "borrow_main_attribute": {
                    "type": "string",
                    "enum": ["Form", "All"],
                    "description": "Опционально для форм: заимствовать ли реквизиты основного объекта",
                },
            },
            "required": ["object"],
        },
    },
    {
        "name": "cfe_patch_method",
        "description": (
            "СОЗДАНИЕ ПЕРЕХВАТЧИКА метода в заимствованном объекте. "
            "Генерирует BSL-функцию с правильным декоратором &Перед/&После/&ИзменениеИКонтроль и "
            "именем процедуры с префиксом расширения. Объект должен быть заимствован cfe_borrow заранее.\n"
            "ВАЖНО: cfe_patch_method создаёт только КАРКАС функции. Тело надо дописать через "
            "read_file → edit_file/write_file."
        ),
        "input_schema": {
            "type": "object",
            "properties": {
                "module_path": {
                    "type": "string",
                    "description": (
                        "Путь к модулю: 'Catalog.X.ObjectModule' / 'Catalog.X.ManagerModule' / "
                        "'Catalog.X.Form.ФормаЭлемента' / 'CommonModule.X' / "
                        "'Document.X.ObjectModule' и т.п."
                    ),
                },
                "method_name": {"type": "string", "description": "Имя оригинального метода (например 'ПриЗаписи', 'ОбработкаПроведения')"},
                "interceptor_type": {
                    "type": "string",
                    "enum": ["Before", "After", "ModificationAndControl"],
                    "description": "Before = &Перед, After = &После, ModificationAndControl = &ИзменениеИКонтроль (копия тела с маркерами #Вставка/#Удаление)",
                },
                "context": {
                    "type": "string",
                    "description": "Директива контекста: 'НаСервере' (по умолчанию), 'НаКлиенте', 'НаКлиентеНаСервереБезКонтекста'",
                },
                "is_function": {
                    "type": "boolean",
                    "description": "True если перехватываемый метод — функция (добавит Возврат в каркас)",
                },
            },
            "required": ["module_path", "method_name", "interceptor_type"],
        },
    },
    {
        "name": "cfe_validate",
        "description": (
            "ПОЛНАЯ ВАЛИДАЦИЯ расширения: XML-формат, свойства, состав, заимствованные объекты, "
            "согласованность Configuration.xml с файлами на диске. "
            "ОБЯЗАТЕЛЬНО вызывать ПЕРЕД db_load. Заменяет validate_ext_structure."
        ),
        "input_schema": {"type": "object", "properties": {}, "required": []},
    },
    {
        "name": "platform_doc_lookup",
        "description": (
            "СТРОГАЯ платформенная справка 1С — официальная документация по методам, "
            "свойствам, событиям, объектам платформы. Источник: shcntx_ru.hbk от 1С, "
            "поставляется вместе с платформой, проиндексирован в Agenter (~25000 записей).\n\n"
            "ОБЯЗАТЕЛЬНО вызывай ПЕРЕД использованием ЛЮБОГО платформенного:\n"
            "  • Метода объекта (Заблокировать, Записать, Очистить, ...)\n"
            "  • Свойства (ИсточникДанных, Режим, Период, ...)\n"
            "  • События (ПередЗаписью, ОбработкаПроведения, ...)\n"
            "  • Объекта (БлокировкаДанных, НаборЗаписей, Запрос, ...)\n\n"
            "Стратегия поиска (lookup внутри сам это делает):\n"
            "  1. Точное совпадение имени_ru или имени_en\n"
            "  2. Case-insensitive\n"
            "  3. Подстрока в имени или полном пути\n"
            "  4. FTS5 fallback по описанию\n\n"
            "Возвращает: name, kind (method/property/object/event), full_path, parent, "
            "signature, description, params, returns, example, availability.\n\n"
            "Если found=0 — метод/свойство НЕ существует в платформе. НЕ ВЫДУМЫВАЙ — "
            "переключись на bsl_search_function (может это БСП) или спроси пользователя."
        ),
        "input_schema": {
            "type": "object",
            "properties": {
                "name": {
                    "type": "string",
                    "description": "Имя на русском или английском, без скобок и параметров. "
                                   "Примеры: 'БлокировкаДанных', 'ИсточникДанных', 'НаборЗаписей.Записать', 'Lock'",
                },
                "limit": {
                    "type": "integer",
                    "default": 5,
                    "description": "Макс. количество результатов (часто одно имя имеет несколько контекстов: метод регистра + метод документа)",
                },
            },
            "required": ["name"],
        },
    },
]


# ---------------------------------------------------------------------------
# State (in-memory, достаточно для MVP)
# ---------------------------------------------------------------------------

class AppState:
    def __init__(self):
        self.desktop_ws: WebSocket | None = None
        self.web_clients: set[WebSocket] = set()
        self.pending_calls: dict[str, asyncio.Event] = {}
        self.call_results: dict[str, Any] = {}
        self.desktop_config: dict = {}   # конфиг клиента, присылается при подключении


state = AppState()


# ---------------------------------------------------------------------------
# Database (SQLite sync, через to_thread)
# ---------------------------------------------------------------------------

def _init_db():
    conn = sqlite3.connect(DB_PATH)
    conn.execute("""
        CREATE TABLE IF NOT EXISTS tasks (
            id TEXT PRIMARY KEY,
            prompt TEXT NOT NULL,
            status TEXT NOT NULL DEFAULT 'pending',
            log_jsonl TEXT NOT NULL DEFAULT '',
            created_at TEXT NOT NULL,
            completed_at TEXT
        )
    """)
    conn.commit()
    conn.close()


def _create_task(task_id: str, prompt: str) -> dict:
    now = datetime.utcnow().isoformat()
    conn = sqlite3.connect(DB_PATH)
    conn.execute(
        "INSERT INTO tasks VALUES (?,?,?,?,?,?)",
        (task_id, prompt, "pending", "", now, None),
    )
    conn.commit()
    conn.close()
    return {"id": task_id, "prompt": prompt, "status": "pending", "created_at": now}


def _get_task(task_id: str) -> dict | None:
    conn = sqlite3.connect(DB_PATH)
    row = conn.execute("SELECT * FROM tasks WHERE id=?", (task_id,)).fetchone()
    conn.close()
    if not row:
        return None
    return {
        "id": row[0], "prompt": row[1], "status": row[2],
        "log_jsonl": row[3], "created_at": row[4], "completed_at": row[5],
    }


def _update_status(task_id: str, status: str, completed_at: str | None = None):
    conn = sqlite3.connect(DB_PATH)
    if completed_at:
        conn.execute("UPDATE tasks SET status=?, completed_at=? WHERE id=?",
                     (status, completed_at, task_id))
    else:
        conn.execute("UPDATE tasks SET status=? WHERE id=?", (status, task_id))
    conn.commit()
    conn.close()


def _append_log(task_id: str, entry: dict):
    conn = sqlite3.connect(DB_PATH)
    line = json.dumps(entry, ensure_ascii=False)
    conn.execute(
        "UPDATE tasks SET log_jsonl = log_jsonl || ? WHERE id=?",
        (line + "\n", task_id),
    )
    conn.commit()
    conn.close()


# ---------------------------------------------------------------------------
# Broadcast & logging
# ---------------------------------------------------------------------------

async def broadcast(event: dict):
    dead: set[WebSocket] = set()
    for ws in state.web_clients:
        try:
            await ws.send_json(event)
        except Exception:
            dead.add(ws)
    state.web_clients -= dead


async def publish_log(task_id: str, text: str, meta: str = ""):
    ts = datetime.utcnow().strftime("%H:%M:%S")
    entry = {"type": "log", "task_id": task_id, "ts": ts, "text": text, "meta": meta}
    await asyncio.to_thread(_append_log, task_id, entry)
    await broadcast(entry)


# ---------------------------------------------------------------------------
# Relay: Cloud → Desktop → Cloud
# ---------------------------------------------------------------------------

async def relay_tool_call(tool: str, params: dict, timeout: int = 300) -> str:
    if state.desktop_ws is None:
        raise RuntimeError("Desktop-ассистент не подключён")

    call_id = str(uuid.uuid4())
    event = asyncio.Event()
    state.pending_calls[call_id] = event

    try:
        await state.desktop_ws.send_json({
            "type": "tool_call",
            "call_id": call_id,
            "tool": tool,
            "params": params,
        })
    except Exception as e:
        state.pending_calls.pop(call_id, None)
        raise RuntimeError(f"Ошибка отправки tool_call: {e}")

    try:
        await asyncio.wait_for(event.wait(), timeout=timeout)
    except asyncio.TimeoutError:
        state.pending_calls.pop(call_id, None)
        raise TimeoutError(f"Таймаут инструмента '{tool}' ({timeout}s)")

    result = state.call_results.pop(call_id, {})
    if not result.get("ok", False):
        raise RuntimeError(result.get("error", "Неизвестная ошибка от desktop"))

    raw = str(result.get("result", ""))
    return raw[:MAX_TOOL_RESULT]


# ---------------------------------------------------------------------------
# LLM Orchestrator
# ---------------------------------------------------------------------------

async def run_task(task_id: str, prompt: str):
    await asyncio.to_thread(_update_status, task_id, "running")
    await broadcast({"type": "status", "task_id": task_id, "status": "running"})
    await publish_log(task_id, "Задача принята, запускаю агента...")

    if not ANTHROPIC_API_KEY:
        await asyncio.to_thread(_update_status, task_id, "error", datetime.utcnow().isoformat())
        await broadcast({"type": "task_error", "task_id": task_id,
                         "error": "ANTHROPIC_API_KEY не установлен"})
        return

    client = anthropic.AsyncAnthropic(api_key=ANTHROPIC_API_KEY)
    messages: list[dict] = [{"role": "user", "content": prompt}]

    try:
        for iteration in range(MAX_ITERATIONS):
            await publish_log(task_id, f"── Итерация {iteration + 1}/{MAX_ITERATIONS}")

            response = await client.messages.create(
                model="claude-sonnet-4-6",
                max_tokens=8096,
                system=[{
                    "type": "text",
                    "text": _build_system_prompt(state.desktop_config),
                    "cache_control": {"type": "ephemeral"},
                }],
                tools=TOOL_DEFINITIONS,
                messages=messages,
            )

            # Логируем текстовые блоки
            for block in response.content:
                if hasattr(block, "text") and block.text:
                    await publish_log(task_id, block.text)

            if response.stop_reason == "end_turn":
                await asyncio.to_thread(
                    _update_status, task_id, "done", datetime.utcnow().isoformat()
                )
                await broadcast({"type": "task_done", "task_id": task_id})
                await publish_log(task_id, "✓ Задача выполнена")
                # Уведомляем desktop-агент (для tray-нотификации)
                if state.desktop_ws:
                    try:
                        await state.desktop_ws.send_json(
                            {"type": "task_done_notify", "task_id": task_id}
                        )
                    except Exception:
                        pass
                return

            if response.stop_reason != "tool_use":
                await publish_log(task_id, f"Неожиданная остановка: {response.stop_reason}")
                break

            # Выполняем tool calls через relay
            tool_results = []
            for block in response.content:
                if block.type != "tool_use":
                    continue

                tool_name = block.name
                tool_input = block.input
                params_preview = json.dumps(tool_input, ensure_ascii=False)[:120]
                await publish_log(task_id, f"→ {tool_name}", params_preview)

                try:
                    result_text = await relay_tool_call(tool_name, tool_input)
                    await publish_log(
                        task_id,
                        f"← {tool_name}: OK",
                        f"{len(result_text)} симв.",
                    )
                    tool_results.append({
                        "type": "tool_result",
                        "tool_use_id": block.id,
                        "content": result_text,
                    })
                except Exception as exc:
                    err_msg = str(exc)
                    await publish_log(task_id, f"← {tool_name}: ОШИБКА", err_msg[:300])
                    if tool_name in CRITICAL_TOOLS:
                        # Критический инструмент завершился с ошибкой —
                        # продолжать бессмысленно, pipeline остановлен
                        raise RuntimeError(
                            f"[{tool_name}] завершился с ошибкой — задача прервана.\n{err_msg}"
                        )
                    tool_results.append({
                        "type": "tool_result",
                        "tool_use_id": block.id,
                        "content": f"ERROR: {err_msg}",
                        "is_error": True,
                    })

            messages.append({"role": "assistant", "content": response.content})
            messages.append({"role": "user", "content": tool_results})

        # Превышен лимит итераций
        await asyncio.to_thread(
            _update_status, task_id, "error", datetime.utcnow().isoformat()
        )
        await broadcast({"type": "task_error", "task_id": task_id,
                         "error": f"Превышен лимит итераций ({MAX_ITERATIONS})"})

    except Exception as exc:
        log.exception("Ошибка выполнения задачи %s", task_id)
        await asyncio.to_thread(
            _update_status, task_id, "error", datetime.utcnow().isoformat()
        )
        await broadcast({"type": "task_error", "task_id": task_id, "error": str(exc)})


# ---------------------------------------------------------------------------
# FastAPI app
# ---------------------------------------------------------------------------

@asynccontextmanager
async def lifespan(_app: FastAPI):
    _init_db()
    log.info("База данных инициализирована: %s", DB_PATH)
    yield


app = FastAPI(lifespan=lifespan, title="Agenter Backend")

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_methods=["*"],
    allow_headers=["*"],
)

# Раздаём frontend/ как статику
_frontend_dir = os.path.join(_AGENTER_ROOT, "frontend")
if os.path.isdir(_frontend_dir):
    app.mount("/ui", StaticFiles(directory=_frontend_dir, html=True), name="ui")


# ---------------------------------------------------------------------------
# REST API
# ---------------------------------------------------------------------------

class TaskRequest(BaseModel):
    prompt: str
    project_id: str = "default"


@app.post("/tasks")
async def create_task(req: TaskRequest, bg: BackgroundTasks):
    task_id = str(uuid.uuid4())
    await asyncio.to_thread(_create_task, task_id, req.prompt)
    bg.add_task(run_task, task_id, req.prompt)
    return {"task_id": task_id, "status": "pending"}


@app.get("/tasks/{task_id}")
async def get_task(task_id: str):
    task = await asyncio.to_thread(_get_task, task_id)
    if not task:
        return {"error": "Задача не найдена"}
    return task


@app.get("/health")
async def health():
    return {
        "status": "ok",
        "desktop_connected": state.desktop_ws is not None,
        "web_clients": len(state.web_clients),
    }


@app.get("/desktop/status")
async def get_desktop_status():
    return {"connected": state.desktop_ws is not None}


# ---------------------------------------------------------------------------
# WebSocket: Desktop Agent
# ---------------------------------------------------------------------------

@app.websocket("/ws/desktop")
async def ws_desktop(ws: WebSocket):
    await ws.accept()
    state.desktop_ws = ws
    await broadcast({"type": "desktop_status", "status": "online"})
    log.info("Desktop подключён")

    try:
        while True:
            data = await ws.receive_json()
            msg_type = data.get("type")

            if msg_type == "hello":
                # Десктоп присылает свой конфиг при подключении
                cfg = data.get("config", {})
                if cfg:
                    state.desktop_config = cfg
                    log.info("Desktop config: ext=%s ext_src=%s",
                             cfg.get("extension", "?"), cfg.get("ext_src_path", "?"))

            elif msg_type == "tool_result":
                call_id = data.get("call_id")
                state.call_results[call_id] = data
                event = state.pending_calls.pop(call_id, None)
                if event:
                    event.set()

            elif msg_type == "log":
                # Desktop шлёт свои логи (stdout скриптов)
                await broadcast(data)

            elif msg_type == "ping":
                await ws.send_json({"type": "pong"})

            elif msg_type == "status":
                await broadcast(data)

    except WebSocketDisconnect:
        pass
    except Exception as exc:
        log.warning("Desktop WS error: %s", exc)
    finally:
        state.desktop_ws = None
        # Разблокируем все ожидающие вызовы с ошибкой
        for call_id, event in list(state.pending_calls.items()):
            state.call_results[call_id] = {"ok": False, "error": "Desktop отключился"}
            event.set()
        state.pending_calls.clear()
        await broadcast({"type": "desktop_status", "status": "offline"})
        log.info("Desktop отключён")


# ---------------------------------------------------------------------------
# WebSocket: Web clients
# ---------------------------------------------------------------------------

@app.websocket("/ws/events")
async def ws_events(ws: WebSocket):
    await ws.accept()
    state.web_clients.add(ws)

    # Сразу сообщаем текущий статус
    await ws.send_json({
        "type": "desktop_status",
        "status": "online" if state.desktop_ws else "offline",
    })

    try:
        while True:
            # Просто держим соединение живым
            await ws.receive_text()
    except WebSocketDisconnect:
        pass
    finally:
        state.web_clients.discard(ws)
